app:
    # Telegram Bot API Token
    bot:
        token: {TOKEN}
        parse_mode: html

    # Modules to load
    # Importing certain modules: handlers.echo
    # Importing certain folder: handlers; handlers/private
    # Importing init: handlers.__init__ OR $handlers (shortcut)
    modules:
        - $handlers
